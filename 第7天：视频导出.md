视频导出方案：

1、网站后台导出mp4文件，然后浏览器下载

+ 优点：对于用户来说体验最好，无需繁琐的操作
+ 缺点：对网站服务器性能要求高，如果很多任务同时进行，服务器并发是个问题

2、前端通过代码逐帧渲染生成mp4文件，然后自动下载

+ 优点：无需服务器，逻辑全部放在前端，节省网站成本，体验也很好
+ 缺点：动画效果会有缺失，尤其是HTML动画，代码在渲染动画的时候可能有些效果丢失

3、前端通过录屏的方式直接录屏生成mp4文件，然后自动下载

+ 优点：无需服务器，逻辑全部放在前端，节省网站成本。所见即所得，不会丢失动画效果。
+ 缺点：需要用户配合操作，稍微繁琐一点。



提示词：

> <font style="color:rgb(26, 28, 31);">本轮实现视频录屏导出的功能：</font>
>
> <font style="color:rgb(26, 28, 31);">1、在预览画布标题栏全屏按钮右侧增加一个视频导出按钮，点击之后弹出让用户选择共享标签页的弹窗。</font>
>
> <font style="color:rgb(26, 28, 31);">2、用户点击确认之后，当前视频进入全屏模式，开始自动播放视频，并进行录屏。</font>
>
> <font style="color:rgb(26, 28, 31);">3、基于WebCodecs技术，通过 </font><font style="color:rgb(26, 28, 31);">MediaStreamTrackProcessor + VideoEncoder + mp4-muxer链路</font><font style="color:rgb(26, 28, 31);">，直接在浏览器里编码 H.264 MP4文件。</font>
>
> <font style="color:rgb(26, 28, 31);">4、录屏结束之后自动下载MP4文件。</font>
>



## 其他问题：
1、如果不知道这条技术方案怎么办？

+ 与AI探讨视频导出方案，让Codex或者Claude Code基于源码分析当前技术框架适合的方案
+ 直接找到对标，让AI分析，比如直接分析svganimate实现原理



2、Agent对话记忆问题，比如用户先发送一条：“把第二个分镜画面标题改成红色”，系统调整之后，用户不满意，又发送一句“还是改成黄色吧”，这种时候，AI研判意图可能就会无法识别。

需要发送历史对话记录，最简单做法：1+N。把用户最开始需求和最近N轮对话的双方交互消息发给AI，以便于AI做意图研判。



3、成本控制问题。可以采用高低模型搭配来完成视频创作。比如分镜大纲生成、意图研判这种可以使用快速便宜的模型，而画面生成这种追求效果的可以使用高质量模型。

