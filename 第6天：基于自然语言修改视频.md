## 第一轮：重新生成大纲
提示词：

> 本轮实现重新生成视频大纲功能：
>
> 用户在右侧对话面板发送消息后，系统会调用AI完成意图分析，如果用户意图是重新生成视频大纲，则根据以下两种情况做不同处理：
>
> + 如果当前还没有任何分镜画面、旁白音频生成，则直接把当前视频的**原始用户提示词+已有的分镜大纲内容+用户新的修改需求**作为上下文一起发给AI，重新生成大纲，生成之后覆盖现有大纲。并同样以消息气泡+表格形式回复用户。
> + 如果当前已经有部分分镜画面、旁白音频生成，需回复一个确认消息气泡，提醒用户当前操作会导致已有数据被覆盖。消息气泡footer区域增加一个确认按钮，用户点击确认之后，开始重新生成大纲。另外后续对话面板有新的消息对话产生之后，之前的消息气泡footer按钮就隐藏，防止用户对过时的消息进行点击操作。
>

## 第二轮：修改分镜内容
提示词：

> 本轮实现修改分镜内容的功能：
>
> 1、调整之前的AI意图分析功能，如果用户的意图是调整某个分镜内容，除了返回用户意图之外，还需要在JSON中返回用户想要调整的分镜编号，暂不支持一次调整多个分镜。
>
> 2、实现修改分镜的占位函数，根据上面返回的待修改分镜编号，调用AI重新生成这个分镜的画面。
>
> 3、调用AI重新生成画面的时候需要携带的上下文内容：
>
> + 当前分镜的原始提示词
> + 当前分镜的旁白
> + 当前分镜已有的HTML源码（仅限HTML动画模式，图片轮播视频除外）
> + 用户这一次输入的修改需求
>
> 4、重新生成之后，以消息气泡形式提醒用户工作完成。
>



## 第三轮：新增分镜
提示词：

> 本轮实现新增分镜的功能：
>
> 1、调整之前的AI意图分析功能，如果用户的意图是新增分镜内容，除了返回用户意图之外，还需要在JSON中返回用户想要在哪个分镜后面插入新的分镜，返回这个分镜的编号。如果用户没有指定编号，则返回0。
>
> 2、实现新增分镜的占位函数，调用AI生成一个新的分镜，包括新分镜的标题、旁白、画面提示词。
>
> 3、在生成新分镜的时候，需要把当前视频的逐字稿、已有的分镜大纲、用户新分镜的需求提示词作为上下文传给AI，便于AI理解当前视频信息和用户对于新增分镜的需求。
>
> 4、如果第一步返回的编号是0，也就是用户没有指定要在哪个分镜后面插入新分镜，则在生成新分镜的时候，让AI根据上下文信息返回适合插入的位置。
>
> 5、把新的分镜插入到大纲中对应的位置（这个位置要么是第一步分析意图的时候提取出来的，要么是生成新分镜的时候AI返回的），插入之后注意调整现有分镜的编号，项目中相关的JSON字段编号要做调整。
>
> 6、继续调用AI生成这个新分镜的画面（图片或HTML动画）、声音。
>
> 7、通过消息气泡形式提醒用户操作完成。
>





其他指令：

+ 开始生成视频
+ 删除某个分镜
+ ···



## 
