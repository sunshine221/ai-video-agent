## 第一轮：生成分镜图片
图片模型：

+ GPT Image2 && Nano banana2：
    - 优点：生成能力强大，几乎没有AI味儿，可以达到以假乱真的程度。文字生成能力好。
    - 缺点：价格昂贵，单张图片成本都是几毛钱。
    - 如果视频对文字生成能力要求高，比如知识讲解领域，可以选择这个。
+ z-image-turbo：
    - 优点：超高的性价比，每张图只需要2.6分，注意是分，不是角，10张图才2毛六。100张图才2块6，相当划算了。
    - 缺点：有AI味儿，文字生成能力差，适用于对文字要求没那么高的场景。
    - 地址：[https://evolink.ai/z-image-turbo](https://evolink.ai/z-image-turbo)



提示词：

> 本轮实现生成图片轮播视频模式下分镜画面功能：
>
> 1、在分镜列表按钮区域增加两个新的按钮：一键生成、中断生成。当视频大纲不存在的时候，按钮隐藏，大纲生成之后，按钮出现。
>
> 2、点击一键生成按钮，开始逐个生成每个分镜的图片：使用分镜所属的画面提示词，调用AI生成图片。同时“中断生成”按钮从禁用状态变为可点击状态。
>
> 3、如果之前已经有部分分镜的画面已经存在，一键生成的时候就跳过。
>
> 4、在生成过程中，用户可以随时点击“中断生成”按钮中断生成流程。已经生成的保存，还未生成的不再继续。
>
> 5、图片具体生成的方法参考这个地址里面的API介绍：[https://evolink.ai/z-image-turbo](https://evolink.ai/z-image-turbo) ，生成图片包含两阶段流程：第一阶段创建生成任务，第二阶段周期性轮询生成结果，目前选择2秒轮询一次。
>
> 6、生成的图片保存在本地项目文件夹中，新建一个文件夹专门用于图片存储，按项目分门别类管理，不要把所有项目图片混在一起。
>
> 7、以JSON格式把项目各分镜图片的路径信息保存起来，存储到项目project表的video_source字段中。
>
> 8、图片生成之后，在分镜列表小卡片上半部分预览区域内要加载出来。点击分镜小卡片，上方的预览大面板展示这个分镜的画面。
>



## 第二轮：生成旁白音频
+ 方案1：直接使用大模型TTS，比如`gpt-4o-mini-tts`模型。
    - 优点：直接使用AI中转站，和AI API共同使用，无需再额外接入一套，维护管理方便
    - 缺点：价格比起方案2稍微贵一点
+ 方案2：使用微软的Azure TTS
    - 优点：音色丰富，并且真人感比较好，而且价格比较便宜
    - 缺点：需要绑定信用卡才能使用

提示词：

> 本轮实现生成旁白音频功能：
>
> 1、修改现有生成分镜画面的逻辑，生成完画面之后，需要调用AI完成分镜旁白音频的生成。
>
> 2、调用gpt-4o-mini-tts模型完成语音生成，复用现有的AI_BASE_URL指向的AI服务即可。音色名称放在.env环境变量中配置，你可以给我设置一个默认的中文声音。
>
> 3、生成的音频文件和图片文件一样，放在本地项目文件夹中，同样按照项目分门别类存放。
>
> 4、将音频文件的路径信息更新到项目project表的video_source字段中。
>
> 5、在分镜小卡片内部右上角增加一个喇叭图标，当该分镜已有音频文件时，图标变为橙色，点击喇叭图标播放音频文件，否则设置为灰色，不可点击。
>



## 第三轮：如何让画面风格保持一致性
+ 拆分分镜大纲的时候就把全局的画风给定下来，每个分镜图片生成的时候把风格提示词带上。
+ 携带相邻分镜图片让AI做参考

提示词：

> 修改生成大纲的环节，在图片轮播视频模式下，让AI根据视频脚本选择合适的画面风格，生成一组全局的图像风格提示词，并添加到每个分镜画面提示词前面，使得画面风格保持一致性。
>

## 第四轮：增加转场效果与实现播放效果
提示词：

> 本轮实现图片轮播视频的播放效果：
>
> 1、点击进度条区域左侧的播放按钮，开启视频播放。
>
> 2、从第一个分镜开始播放分镜的图片，图片停留时长根据对应分镜的音频时长决定。
>
> 3、若分镜音频不存在，则默认展示3秒钟。
>
> 4、进度条右侧的分镜数量改为视频时长和播放时长。比如：00:32/01:34，前面是当前播放的进度，后者是视频总时长。
>
> 5、视频总时长由所有分镜音频时长相加得到，没有音频的分镜时长默认是3秒。
>
> 6、图片在播放过程中，有一个缓慢放大的效果，看上去是镜头缓慢推进的样子。
>
> 7、分镜之间图片切换，采取叠化或淡入淡出的效果。
>





## 第五轮：增加字幕和灵活修改功能
提示词：

> 1、给图片轮播视频增加字幕功能，字幕在画面靠近底部居中的位置。
>
> 2、将分镜的旁白拆分成多个短句，去掉句末的标点符号。
>
> 3、每个短句字数在整个分镜旁白字数中所占的百分比，就是这句话在分镜旁白音频中播放的百分比，通过这种计算方式，让音频播放和字幕显示保持同步。
>
> 4、字幕文字增加阴影显示，字号不要太小。
>
> 5、在编辑分镜的弹窗里面，分镜卡片里需要展示当前分镜的画面。同时也增加一个喇叭图标，用户点击播放当前分镜音频
>
> 6、这个弹窗里面，每个分镜卡片里面新增两个按钮：重新生成画面、重新生成旁白。点击按钮可以重新生成。
>

