## 第一轮：完成项目初始框架搭建
提示词：

> 开发一个AI视频制作网站： 
>
> 1、支持两种视频创作模式：图片轮播模式、HTML视频模式。 
>
> 2、用户先选择创作模式，然后输入提示词，系统完成脚本生成、分镜拆分、分镜画面生成、分镜旁白语音生成、背景音乐选择，播放预览、录屏导出视频。 
>
> 3、图片轮播模式是指分镜画面是由系统调用文生图AI模型生成图片，多个图片轮播形成视频。 
>
> 4、HTML视频模式是指分镜画面是由系统调用AI生成网页动画，多个网页动画形成视频。 
>
> 5、首页展示品牌logo、名称、两种创作模式卡片，点击卡片中的创作按钮进入创作页面。 
>
> 6、创作页分为三栏展示，左侧侧边栏展示历史项目列表，中间大面积区域是视频预览区域，右侧是与AI交互对话的对话界面。 
>
> 7、视频预览区域上方是画面预览，下方是一组小卡片组成的分镜列表，横向摆放，点击小卡片，上方预览区域就播放对应分镜的内容。 
>
> 8、视频预览区域上方有一个工具栏，工具栏里面有一些按钮，比如字幕开关、全屏按钮、录屏导出视频按钮。
>
> 9、本轮只生成前端UI，暂不生成具体的视频创作相关逻辑。
>
> 10、使用Next.js技术栈。
>
> 11、前端部分参考我的截图来设计UI样式，采用浅色主题，而不要采用深色主题。
>



## 第二轮：完成项目后端项目管理功能
提示词：

> 实现项目管理的功能：
>
> 1、使用MySQL管理项目信息，在.env文件中添加MySQL连接信息相关的环境变量，我会来填充相关的值。
>
> 2、用户点击首页两个创作模式中的按钮之时，需要先在数据库中创建一个项目，页面进入Loading状态，直到项目创建成功，再跳转到创作页面。
>
> 3、创作页面左侧历史列表要加载真实的项目数据
>
> 4、数据库中新建一个project表用于存储项目信息，字段包括uuid、标题、创建时间、类型（图片轮播、HTML动画）、完整的分镜大纲内容（大字符串，会包含比较多信息）、视频源码（也是大字符串）
>
> 5、把相关的建表SQL语句输出给我。
>



## 第三轮：完成Agent核心工作框架
提示词：

> 实现视频创作Agent核心逻辑：用户意图分析功能。
>
> 1、用户在创作页面右侧对话栏下方可以输入提示词
>
> 2、新增后端接口接收用户发送的提示词内容，并通过AI大模型对用户输入的提示词做意图分析。
>
> 3、模型调用需支持第三方baseUrl指定，使用OpenAI兼容接口调用。相关的baseUrl、apikey、模型名称配置在.env文件中。模型名默认采用gemini-3-flash-preview。
>
> 4、意图分析实现方法：
>
> 在给AI的系统提示词中告知AI：你是一个视频创作智能体，现在需要对用户输入的指令进行研判，分析用户的行为属于以下哪种情况（未来还可能会扩展新的action值），并给出理由，以结构化的JSON形式返回（并附一个JSON示例给AI做输出参考）。
>
> + 生成视频大纲（包含完整脚本、分镜拆分列表，适用于用于要创作视频，或者直接给出一段视频脚本的情况）
> + 重新生成视频大纲
> + 新增一个分镜
> + 删除一个分镜
> + 重新生成某个分镜
> + 其他不支持的指令
>
> 5、收到AI分析结果之后进行JSON格式校验，如果格式校验错误，打印错误日志。
>
> 6、后端为上面每一种意图分别实现一个功能函数，目前都统一给前端返回一条消息，展示各自不同的意图内容文字即可，未来会根据不同意图执行不同的功能。
>
> 7、前端通过消息气泡的形式展示用户和系统返回的消息。
>
> 8、需要新建一表用于存储用户和系统的对话历史消息，点击创作页面左侧历史项目的时候，右侧对话列表需要自动加载历史对话内容。把相关的建表SQL语句输出给我。
>



<!-- 这是一张图片，ocr 内容为：用户 AILLM AGENT 用户意图分析 用户提示词 用户提示词 分析结果 执行对应操作 执行结果反馈 用户新的提示词 -->
![](https://cdn.nlark.com/yuque/0/2026/png/48011955/1779626245744-e7c4fba7-9821-452a-917d-2966a61418ae.png)



## 第四轮：完成视频脚本与分镜拆分工作
提示词：

> 本轮实现视频大纲生成功能：  
1、实现之前的生成视频大纲的占位功能函数，调用AI模型完成视频大纲生成。
>
> 2、需要在系统提示词中告知AI，根据用户输入的提示词，生成完整的视频脚本逐字稿、然后将这份逐字稿拆分成多个分镜（6-30个），每个分镜需要包含这个分镜的标题、旁白和画面提示词，其中画面提示词用于后续继续调用AI生成分镜画面使用。
>
> 3、根据用户当前选择的模式不同，画面提示词会不一样。如果是图片轮播视频，则需要生成图片生成的提示词。如果是HTML动画视频，则需要生成网页动画的提示词。
>
> 4、当系统研判用户意图发现是要生成视频大纲，则调用这个功能函数，调用AI完成大纲内容生成，同样要求AI以结构化JSON格式返回内容，以便于系统解析处理。
>
> 5、AI返回视频大纲之后，需要返回给前端，前端在消息气泡中以卡片形式展示大纲内容。卡片中间有一个表格展示大纲中每个分镜情况（包括编号、标题、旁白）。
>
> 6、消息卡片有一个footer，里面展示一个“查看完整大纲内容”的按钮，点击之后以弹窗形式展示完整的分镜信息。这个弹窗里面以卡片形式展示每个分镜，每个卡片里面包含分镜画面预览（预留，后续会展示画面）、分镜旁白、分镜提示词等内容。
>





UI布局调整：

1、调整创作页面，右侧对话面板和中间预览面板之间宽度可调整，最大调整为1:1大小

2、中间预览面板去掉假数据，一开始是空，然后根据生成的视频大纲内容，去动态加载分镜信息

