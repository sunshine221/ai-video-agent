## 第一轮：实现分镜动画
提示词：

> 本轮实现HTML视频的功能，如果用户创建的是HTML视频而不是图片轮播视频，这种模式下：
>
> 1、修改生成生成视频大纲功能，修改系统提示词中AI角色设定为“你是一个网页动画视频设计工程师···”，生成的分镜画面提示词不再是图片生成提示词，而是网页动画提示词。
>
> 2、每一个分镜画面对应一个独立的HTML网页，在生成分镜网页动画的时候，要求AI通过HTML、CSS、JS、SVG、Canvas等技术实现一个网页动画用于承载这一个分镜的画面。
>
> 3、要求AI保持视觉风格一致性，把上一个分镜的HTML代码附加在上下文中供AI生成动画时候参考。
>
> 4、分镜动画生成之后，分镜列表区域小卡片上半部分预览区域、编辑分镜弹窗里的卡片动画预览也要同步渲染动画。
>
> 5、点击播放视频按钮的时候，完善网页动画播放逻辑，实现淡入淡出的转场效果。
>
> 6、每个分镜的HTML代码需要保存到项目project表的视频源码字段中，同样以JSON形式存储。
>





## 第二轮：优化动画效果并增加多种视觉风格
第一步：使用Google AI Studio来生成动画风格：

提示词：

> 做一个HTML动画视频风格展示器，内置3套不同的主题风格。
>
> 1、左侧是一个动画预览面板，右侧包含一组风格按钮。
>
> 2、点击不同按钮在左侧展示对应风格的HTML视频动画效果（每种风格各做一个demo用于展示）。
>
> 3、风格按钮组下方有一个面板，里面显示的是对应风格的提示词，用户可以复制这个提示词用来生成同种风格的动画。提示词只限定风格，而不要出现具体的动画内容。比如可以规定字体、字号、布局、颜色等。
>
> 4、风格需要适合知识讲解、科技博主等场景。生成的动画要作为视频画面，禁止使用网页、PPT那样的设计思路，要贴合视频的展示效果。文字要大，无关的杂项内容不要出现，画面干净，效果专业。
>



第二步：集成风格到系统中

提示词：

> 本轮新增HTML视频风格选项功能：
>
> 1、这个项目里有三套视觉风格：/Users/xuanyuan/Documents/编程技术宇宙/知识星球/AI编程训练营-视频制作智能体开发/html动画视频风格展示器，将里面的视觉风格提示词提取出来，集成到系统中，每个风格一个文件。提示词可能需要做适当调整以适合拼接到完整的分镜动画提示词中。
>
> 2、在右侧提示词输入框下方，发送按钮左侧位置，添加一个视觉风格按钮，点击弹窗选择视觉风格。每种风格一个卡片，卡片中展示上面那个项目里面对应的动画demo。
>
> 3、用户选择视觉风格之后，后续在生成分镜的HTML动画的时候，需要把对应视觉风格提示词一并带上，确保AI按照所选风格来生成动画。
>
> 4、如果用户没有选择视觉风格，默认采用第一种风格。
>





